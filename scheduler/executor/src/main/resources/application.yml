server:
  port: 7041
spring:
  application:
    name: shceduler-executor
  profiles:
    active: @environment@
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
